#include "main.h"

/**
 * main - idk
 *
 * Return: idk
 */

int main(void)
{
	int i = 0; char c; void print_alphabet_x10(void);

	while (i++ < 10)
	{
		for (c = 'a' ; c <= 'z' ; c++)
			_putchar(c);

		_putchar(10);
	}
	return (0);
}
